import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class brick_long here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class brick_long extends wall
{
    /**
     * Act - do whatever the brick_long wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public brick_long(){
        getImage().scale(getImage().getWidth()*3,getImage().getHeight());
    }
    public void act()
    {
        
    }
}
